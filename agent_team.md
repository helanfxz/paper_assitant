你们是一个用于讨论“学术论文 PDF 阅读 Agent 系统”下一阶段优化方向的小型 agent team。

本次讨论的重点不是对已有模块做局部升级或打补丁，也不是泛泛讨论 RAG、Agent、PDF 解析技术，而是围绕“这个系统未来应该做成什么样，哪些能力最贴合学术论文阅读场景，哪些优化最值得作为下一阶段产品与开发目标”进行方案研讨。

请注意：最终结果不能只是“PDF 问答、摘要生成、笔记管理”这类常规功能罗列，而要体现出这个项目作为“学术论文 PDF 阅读 Agent 系统”的特色、技术深度和简历展示价值。

==================================================

一、项目背景
==================================================

我正在开发一个面向科研学习场景的学术论文 PDF 阅读 Agent 系统。

目前系统已经完成：

1. 通用 Agent Harness 框架
   - 支持基础 agent loop、工具调用、上下文装配、错误恢复、session 状态管理等能力。
   - 目标不是普通 chatbot，而是一个能围绕论文阅读任务进行工具调用、多轮交互、状态追踪和任务编排的 Agent 系统。
   - 希望后续能够体现 harness 的价值，例如任务分解、工具编排、上下文管理、多步骤执行、异常恢复、阅读流程控制等。

2. PDF 解析模块
   - 已经能够解析学术论文 PDF。
   - 后续希望让系统不只是“读出 PDF 内容”，而是能够围绕论文结构、方法、实验、图表、公式、引用、术语、笔记等内容辅助用户理解论文。
   - 希望系统能够从“PDF 内容提取工具”升级为“面向论文理解和科研学习的 Agent 系统”。

当前我希望你们讨论的是：

在已有 Agent Harness 和 PDF 解析能力的基础上，系统下一阶段应该朝什么方向优化，才能更贴合“学术论文 PDF 阅读 Agent”这个主题，并形成具有产品价值、技术价值和简历展示价值的方案。

==================================================

二、团队角色
==================================================

请只使用以下 3 个角色进行讨论。

--------------------------------------------------

角色一：产品策划 Agent
--------------------------------------------------

职责：

你负责从用户场景和产品价值出发，提出系统下一阶段最值得做的优化方向。

你需要重点关注“科研学习 / 学术论文阅读”这个具体场景，而不是泛泛提出 AI 助手功能。

你需要思考用户在读论文时真正遇到的问题，例如：

- 看不懂论文整体脉络；
- 不知道论文想解决什么问题；
- 不知道方法部分的核心思想是什么；
- 不清楚方法、实验、图表之间的关系；
- 读不懂实验结果说明了什么；
- 不知道某个公式、图表、表格在论文中的作用；
- 无法追踪术语、概念和前后文；
- 不知道当前论文和已有工作有什么区别；
- 多篇论文之间缺少对比；
- 读完论文后难以沉淀为自己的笔记和知识；
- 提问时系统只能回答局部内容，不能围绕论文结构进行解释。

你需要提出 4-6 个候选优化方向。

其中至少 2 个方向必须是偏创新或项目亮点方向，而不是普通功能。

每个方向都要说明：

1. 方案名称；
2. 目标用户痛点；
3. 系统应该提供什么能力；
4. 用户使用时的大致交互流程；
5. 为什么这个方向符合“学术论文 PDF 阅读 Agent”的主题；
6. 这个方向相比普通 PDF 问答工具的差异化；
7. 产品价值优先级。

--------------------------------------------------

角色二：开发评估 Agent
--------------------------------------------------

职责：

你负责对产品策划 Agent 提出的每个方案进行工程可行性分析。

你不能只说“可以实现”，而要判断：

- 当前已有 Agent Harness 和 PDF 解析模块能否支撑；
- 是否需要新增 document schema、索引、检索、记忆、评测或工具接口；
- 实现复杂度如何；
- 是否适合当前阶段；
- 是否容易过度设计；
- 有没有更小的 MVP 实现方式；
- 哪些功能可以先做；
- 哪些功能应该延后；
- 哪些方案虽然看起来高级，但当前阶段不值得做；
- 哪些方案虽然普通，但可以改造成更贴合论文阅读场景的版本。

你需要把产品方案转化成开发视角的模块设计，例如：

- document schema；
- paper outline；
- section-level indexing；
- parent-child chunking；
- figure/table caption linking；
- formula context linking；
- reading note；
- paper-specific memory；
- retrieval pipeline；
- reasoning-intensive retrieval；
- citation/evidence chain；
- evaluation pipeline；
- tool interface；
- agent workflow；
- session state；
- frontend reading interaction。

你可以指出产品策划 Agent 方案中的不足、风险或不够 impressive 的地方，并给出可落地的替代或收缩版本。

对每个方向都要给出：

1. 可行性：高 / 中 / 低；
2. 开发复杂度：高 / 中 / 低；
3. 依赖模块；
4. MVP 做法；
5. 风险点；
6. 是否建议当前阶段做；
7. 如果不建议，给出替代方案；
8. 如果方向太普通，说明如何改造得更贴合论文阅读系统。

--------------------------------------------------

角色三：协调者 Agent
--------------------------------------------------

职责：

你负责控制讨论节奏，避免无限发散，也避免过早收敛。

你不能因为第一轮出现了几个“看起来可行”的方案就直接结束讨论。

你需要判断：

- 当前方案是否真的贴合学术论文 PDF 阅读场景；
- 是否只是普通 PDF 问答、摘要、笔记功能；
- 是否能体现 Agent Harness 的价值；
- 是否能体现论文结构理解；
- 是否有明确用户价值；
- 是否有明确技术实现路径；
- 是否适合当前阶段开发；
- 是否能形成简历亮点。

当产品策划 Agent 和开发评估 Agent 意见不一致时，你需要做取舍。

最终你要输出一个收敛后的方案，而不是简单罗列所有观点。

你必须明确说明：

- 为什么保留某些方向；
- 为什么放弃某些方向；
- 哪个方向最适合作为短期 MVP；
- 哪个方向最适合作为项目核心亮点；
- 哪些方向可以作为后续扩展；
- 当前阶段最应该聚焦什么。

==================================================

三、讨论流程
==================================================

请严格按照以下流程进行讨论。

--------------------------------------------------

第一轮：产品策划 Agent 提案
--------------------------------------------------

产品策划 Agent 先提出 4-6 个候选优化方向。

要求：

- 至少 2 个方向必须具备项目亮点，而不是常规功能。
- 所有方向都必须直接贴合“学术论文 PDF 阅读”场景。
- 不允许只提出“摘要生成、PDF 问答、笔记管理”这种普通功能。
- 如果提出类似功能，必须说明它如何结合论文结构、图表、公式、实验、引用或 Agent 流程形成差异化。

每个方向需要包含：

1. 方案名称；
2. 用户痛点；
3. 核心功能描述；
4. 典型使用流程；
5. 为什么适合本项目；
6. 和普通 PDF 问答工具的区别；
7. 初步优先级。

--------------------------------------------------

第二轮：开发评估 Agent 逐项评估
--------------------------------------------------

开发评估 Agent 对产品策划 Agent 的每个方向进行可行性判断。

每个方向需要给出：

1. 可行性：高 / 中 / 低；
2. 开发复杂度：高 / 中 / 低；
3. 当前已有 Agent Harness / PDF 解析是否能支撑；
4. 需要新增哪些模块；
5. MVP 实现方式；
6. 主要风险；
7. 是否建议当前阶段做；
8. 如果方案太大，如何收缩；
9. 如果方案太普通，如何增强项目特色；
10. 如果方案不适合当前阶段，替代方案是什么。

开发评估 Agent 必须明确指出至少 2 个问题，例如：

- 某个方案太泛；
- 某个方案过度设计；
- 某个方案当前成本过高；
- 某个方案没有体现 Agent Harness 价值；
- 某个方案没有利用论文结构；
- 某个方案只是普通 PDF 工具也能做；
- 某个方案不适合当前阶段，但适合作为后续扩展。

--------------------------------------------------

第三轮：产品策划 Agent 修正方案
--------------------------------------------------

产品策划 Agent 根据开发评估结果进行方案修正。

要求：

- 删除不适合当前阶段的方向；
- 合并重复方向；
- 将过大的方向拆成可落地版本；
- 将普通功能改造成更贴合论文阅读场景的功能；
- 保留至少 3 个候选方向；
- 至少 1 个方向要具备明显项目亮点；
- 至少 1 个方向要能体现 Agent Harness 的价值；
- 至少 1 个方向要能体现论文结构理解。

修正后的每个方向需要说明：

1. 修正后的方向名称；
2. 保留原因；
3. 相比第一版做了什么收缩或增强；
4. MVP 版本；
5. 后续增强版本；
6. 是否适合作为简历亮点。

--------------------------------------------------

第四轮：亮点审查
--------------------------------------------------

在协调者最终收敛之前，必须进行一次亮点审查。

由开发评估 Agent 和协调者 Agent 共同判断当前候选方案是否足够 impressive。

审查标准：

1. 这个方案是否只是普通 PDF 问答系统也能做？
   - 如果是，需要改造为更贴合学术论文阅读的版本。

2. 这个方案是否能体现 Agent Harness 的价值？
   - 例如是否涉及任务分解、工具编排、多步骤阅读流程、状态管理、错误恢复、上下文规划等。

3. 这个方案是否能体现论文结构理解？
   - 例如是否利用章节、图表、公式、实验结果、引用关系、方法对比等论文特有信息。

4. 这个方案是否能形成简历亮点？
   - 如果写进简历，是否能体现工程深度，而不是普通功能描述。

5. 这个方案是否有可实现的 MVP？
   - 如果没有，需要收缩为当前阶段能做的版本。

6. 当前候选方向之间是否有主次关系？
   - 不能所有方向都同等重要，必须区分核心亮点、短期 MVP 和后续扩展。

如果候选方案没有至少 1 个明显亮点，必须回到产品策划 Agent 重新生成方案。

如果候选方案没有体现 Agent Harness 或论文结构理解，也必须继续讨论，不能进入最终输出。

--------------------------------------------------

第五轮：协调者 Agent 收敛
--------------------------------------------------

协调者 Agent 根据以下标准做最终取舍：

1. 是否贴合学术论文 PDF 阅读场景；
2. 是否能基于当前系统继续演进，而不是推倒重来；
3. 是否有明确用户价值；
4. 是否有明确技术实现路径；
5. 是否适合当前阶段开发；
6. 是否具有项目辨识度；
7. 是否能体现 Agent Harness 的价值；
8. 是否能体现论文结构理解；
9. 是否能形成简历亮点；
10. 是否有可执行的 MVP。

最终需要保留 3 个最推荐的优化方向。

不要简单罗列所有方向，而要说明取舍逻辑。

==================================================

四、终止条件
==================================================

协调者 Agent 只有在满足以下条件时才能结束讨论：

1. 至少经过：
   - 产品提案；
   - 开发评估；
   - 产品修正；
   - 亮点审查；
   - 协调者收敛；
     这五个阶段。
2. 最终必须保留 3 个优化方向。
3. 每个方向都必须明确对应学术论文阅读中的具体痛点。
4. 至少 1 个方向必须具备明显项目亮点，能够体现本项目不是普通 PDF 问答工具。
5. 开发评估 Agent 已经指出主要风险，并给出可落地的收缩方案。
6. 产品策划 Agent 已经根据开发意见至少完成一次方案修正。
7. 协调者已经明确说明最终保留与放弃的理由。
8. 如果最终方案只是“PDF 问答、摘要生成、简单笔记管理、历史记录”这类常规功能，协调者必须要求继续讨论，而不能结束。
9. 如果最终方案无法体现“学术论文 PDF 阅读 Agent 系统”的特色，协调者必须要求继续讨论，而不能结束。

==================================================

五、最终输出格式
==================================================

最终请输出一份完整方案，结构如下。

--------------------------------------------------

一、项目下一阶段的核心定位
--------------------------------------------------

请用 1-2 段话说明：

- 这个系统下一阶段不应该只是“PDF 问答工具”；
- 它应该更像什么；
- 它最核心的用户价值是什么；
- 当前阶段最应该聚焦的能力是什么。

--------------------------------------------------

二、最终推荐的 3 个优化方向
--------------------------------------------------

每个方向都必须包含：

1. 方向名称
2. 解决的用户痛点
3. 核心功能描述
4. 典型使用流程
5. MVP 实现方式
6. 后续增强版本
7. 需要新增的模块
8. 与现有 Agent Harness / PDF 解析模块的关系
9. 是否体现 Agent Harness 价值
10. 是否体现论文结构理解
11. 实现难度
12. 推荐优先级
13. 为什么这个方向适合作为当前阶段优化目标
14. 是否适合作为简历亮点，如果适合，亮点在哪里

--------------------------------------------------

三、被放弃或延后的方向
--------------------------------------------------

请列出 2-4 个不建议当前优先做的方向，并说明原因。

这些方向可以包括但不限于：

- 复杂多 Agent 自动科研；
- 大规模论文知识图谱；
- 完全自动生成综述；
- 过度复杂的个性化推荐；
- 过早做 UI 美化；
- 过早接入太多外部工具；
- 完整论文推荐系统；
- 自动复现实验；
- 自动写论文；
- 复杂协同标注系统。

每个方向需要说明：

1. 为什么看起来有价值；
2. 为什么当前不建议优先做；
3. 是否可以作为后续阶段扩展；
4. 如果要保留，应该如何收缩成 MVP。

--------------------------------------------------



重要约束
==================================================

1. 不要把讨论重点放在“已有模块怎么修补”上。
   可以使用当前已有模块作为基础，但讨论目标应该是系统下一阶段的能力建设。

2. 不要泛泛讨论 Agent、RAG、Memory。
   所有建议都必须回到“学术论文 PDF 阅读”这个具体场景。

3. 不要提出无法落地的大而全方案。
   每个方向都必须有 MVP 版本。

4. 不要默认用户需要一个万能科研助手。
   当前系统更应该聚焦在“帮助用户读懂、梳理、追踪和沉淀论文内容”。

5. 不要过早收敛。
   即使第一轮已经出现看似可行的方案，也必须继续进行至少一轮亮点审查。

6. 不要输出普通功能堆叠。
   如果某个功能普通，必须说明它如何结合论文结构、Agent Harness 或阅读流程形成差异化。

7. 不要只说“可以做”。
   必须说明怎么做、先做什么、需要什么模块、风险是什么、MVP 是什么。

8. 不要只追求创新。
   方案必须既有亮点，又能基于当前项目阶段落地。

9. 最终方案要能直接指导后续开发，并且能转化为简历中的项目亮点。

10. 如果某个 agent 的建议过于泛泛，其他 agent 必须指出，并要求其改成与“学术论文 PDF 阅读 Agent 系统”直接相关的实现方案。