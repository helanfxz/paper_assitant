# 架构说明文档

本项目包含两个版本的智能文档问答助手，分别对应两种 Agent 架构：

| 文件 | 架构 | 端口 |
|------|------|------|
| `app.py` | ReAct + Plan-and-Execute | 7860 |
| `multi_agent_app.py` | Supervisor + Multi-Agent | 7861 |

---

## 一、app.py — ReAct + Plan-and-Execute 架构

### 架构总览

```
用户输入
    ↓
ask()
    ↓
_plan()  ←── fast_llm 判断问题复杂度
    ↓
[简单问题]              [复杂问题]
    ↓                       ↓
_run_once(question)    for each 子任务:
    ↓                    _run_once(子任务)
[ReAct 图]                  ↓
agent_node              _synthesize()
    ↓                       ↓
should_continue         最终回答
    ↓ (有 tool_calls)
tools_node
    ↓
agent_node (循环)
    ↓ (无 tool_calls)
幻觉检测
    ↓ PASS / 超过3次重试
返回答案
    ↓ FAIL 且 retry < 3
重写问题 → _run_once 重试
```

### 用户操作 → 代码执行路径

---

#### 操作 1：点击「初始化助手」

**触发**：`init_btn.click` → `init_assistant(user_id)`

**执行链**：
1. 创建 `IndustrialPDFLearningAgent(user_id)`
2. 初始化 `OpenAIEmbeddings`、`ChatOpenAI`（glm-5）、`ChatOpenAI`（glm-4-flash）
3. 连接 Qdrant，检查并创建两个集合：
   - `pdf_knowledge`：存储 PDF 子块向量
   - `user_semantic_memory`：存储用户笔记和 QA 历史
4. 调用 `_build_agent()`，构建 ReAct 图并编译（含 MemorySaver checkpointer）
5. 返回初始化成功提示

**ReAct 图结构**（`_build_agent` 内部）：
```
START → agent_node → should_continue → tools_node → agent_node（循环）
                                     → END
```
- `agent_node`：`llm.bind_tools([search_pdf, recall_memory, add_note, get_stats]).invoke(messages)`
- `should_continue`：检查最后一条消息是否有 `tool_calls`
- `tools_node`：`ToolNode` 执行对应工具函数

---

#### 操作 2：上传 PDF 并点击「加载文档」

**触发**：`load_btn.click` → `load_pdf(pdf_file)` → `agent.load_document(pdf_path)`

**执行链**：
1. `pymupdf4llm.to_markdown(pdf_path)`：将 PDF 解析为 Markdown 文本（保留表格、公式结构）
2. `parent_splitter`（chunk_size=1500）切大块，生成父块列表
3. 对每个父块，`child_splitter`（chunk_size=400）切小块
4. 每个小块的 metadata 中写入父块原文（`parent_text`）和父块 ID（`parent_id`）
5. 所有小块向量化后写入 Qdrant `pdf_knowledge` 集合
6. 更新 `stats["docs_loaded"] += 1`

**父子块设计目的**：检索时用小块精确匹配，返回时用父块提供完整上下文（Small-to-Big 策略）。

---

#### 操作 3：在问答框输入问题并发送

**触发**：`send_btn.click` 或回车 → `chat(message, history)` → `agent.ask(question)`

**执行链（详细）**：

**Step 1 — Planning（`_plan`）**
- `fast_llm` 判断问题是否需要分解
- 输出 `SIMPLE` → 空列表，走单次执行
- 输出多行子问题 → 走 Plan-and-Execute 分支

**Step 2A — 简单问题：`_run_once(question)`**

进入最多 4 次的重试循环：

1. 清空 `self._last_retrieved_docs`
2. 调用 ReAct 图：`self.app.invoke({"messages": [HumanMessage(question)]}, config)`
3. ReAct 图内部循环：
   - `agent_node` 调用 `llm.bind_tools`，LLM 决定调用哪个工具
   - 若调用 `search_pdf`：
     - `fast_llm` 生成 3 个扩展查询（MQE）
     - 对 4 个查询各检索 3 个小块（共最多 12 个）
     - 映射到父块去重
     - `fast_llm` 重排，选出最相关片段
     - 结果写入 `self._last_retrieved_docs`，返回给 LLM
   - 若调用 `recall_memory`：Qdrant 按 user_id 过滤检索记忆
   - 若调用 `add_note`：写入 `user_semantic_memory` 集合
   - 若调用 `get_stats`：返回会话统计字符串
   - 若无 tool_calls：退出循环，取最后一条 AIMessage 作为答案
4. 若 `_last_retrieved_docs` 非空（调用了 search_pdf），进行幻觉检测：
   - `fast_llm` 对比答案与检索上下文，输出 `PASS` 或 `FAIL`
   - `PASS` 或 `retry >= 3`：保存 QA 到记忆库，返回答案（FAIL 时追加警告）
   - `FAIL` 且 `retry < 3`：`fast_llm` 重写问题，进入下一轮重试

**Step 2B — 复杂问题：Plan-and-Execute**

1. 对每个子任务依次调用 `_run_once(子任务)`（串行执行）
2. 收集所有 `(子问题, 回答)` 对
3. `_synthesize`：`llm` 综合所有子答案，生成最终连贯回答

**Step 3 — 返回**：答案追加到 Gradio `history`，显示在对话框

---

#### 操作 4：在「学习笔记」Tab 保存笔记

**触发**：`note_btn.click` → `add_note_ui` → `agent.add_note(content, concept)`

**执行链**：
1. 构造 `Document`，metadata 含 `user_id`、`type="note"`、`concept`
2. 写入 Qdrant `user_semantic_memory` 集合（向量化存储）
3. 下次用户问"我之前记了什么"时，`recall_memory` 工具可检索到

---

## 二、multi_agent_app.py — Supervisor + Multi-Agent 架构

### 架构总览

```
用户输入
    ↓
ask()
    ↓
主图：SupervisorState（共享 messages + next）
    ↓
supervisor_node（fast_llm 路由）
    ↓           ↓           ↓           ↓
ResearchAgent  MemoryAgent  GeneralAgent  FINISH→END
（子图）        （子图）      （子图）
    ↓               ↓           ↓
  结果追加       结果追加     结果追加
  到 messages   到 messages  到 messages
    ↓               ↓           ↓
        回到 supervisor_node
                ↓
            FINISH → END
```

每个专家子图内部结构（标准 ReAct）：
```
START → agent_node → should_continue → tools_node → agent_node（循环）
                                     → END
```

### 用户操作 → 代码执行路径

---

#### 操作 1：点击「初始化助手」

**触发**：`init_btn.click` → `init_assistant(user_id)` → `MultiAgentPDFLearningAgent(user_id)`

**执行链**：
1. 初始化 LLM、Embeddings、Qdrant（与 app.py 相同）
2. 调用 `_build_multi_agent_graph()`：
   - `_make_research_tools()`：创建 `search_pdf` 工具（闭包捕获 self）
   - `_make_memory_tools()`：创建 `recall_memory`、`add_note` 工具
   - `_make_general_tools()`：创建 `get_stats` 工具
   - `_build_sub_agent(research_tools, system_prompt)` → 编译 ResearchAgent 子图
   - `_build_sub_agent(memory_tools, system_prompt)` → 编译 MemoryAgent 子图
   - `_build_sub_agent(general_tools, system_prompt)` → 编译 GeneralAgent 子图
   - 构建主图，注册 4 个节点：`supervisor`、`ResearchAgent`、`MemoryAgent`、`GeneralAgent`
   - 编译主图（含 MemorySaver checkpointer）

**主图边结构**：
```
START → supervisor
supervisor --[ResearchAgent]--> ResearchAgent --> supervisor
supervisor --[MemoryAgent]----> MemoryAgent   --> supervisor
supervisor --[GeneralAgent]---> GeneralAgent  --> supervisor
supervisor --[FINISH]---------> END
```

---

#### 操作 2：上传 PDF 并点击「加载文档」

与 app.py 完全相同，`load_document` 方法逻辑一致。

---

#### 操作 3：在问答框输入问题并发送

**触发**：`send_btn.click` → `chat` → `agent.ask(question)`

**执行链（详细）**：

**Step 1 — 进入主图**
- 初始状态：`{"messages": [HumanMessage(question)], "next": ""}`
- 入口边：`START → supervisor`

**Step 2 — supervisor_node 执行**
- 格式化 `state["messages"]` 为对话历史文本
- `fast_llm` 读取历史，输出路由决策（`ResearchAgent` / `MemoryAgent` / `GeneralAgent` / `FINISH`）
- 解析输出，更新 `state["next"]`
- 条件路由跳转到对应节点

**Step 3A — 路由到 ResearchAgent**
- `research_node` 被调用：
  1. 将 `state["messages"]` 传入 ResearchAgent 子图
  2. 子图内 `agent_node`：`llm.bind_tools([search_pdf]).invoke([system_msg] + messages)`
  3. LLM 决定调用 `search_pdf`：
     - MQE 扩展查询 → 多路检索 → 父子块映射 → LLM 重排
     - 结果写入 `self._last_retrieved_docs`，返回给 LLM
  4. LLM 基于检索结果生成回答，无 tool_calls，子图结束
  5. 取子图最后一条 AIMessage，包装为 `AIMessage(content=answer, name="ResearchAgent")`
  6. 追加到主图 `state["messages"]`
- 边：`ResearchAgent → supervisor`，Supervisor 再次执行
- Supervisor 看到 ResearchAgent 已回答，输出 `FINISH`
- 主图结束

**Step 3B — 路由到 MemoryAgent**
- `memory_node` 被调用：
  1. 子图内 LLM 可调用 `recall_memory` 或 `add_note`
  2. `recall_memory`：Qdrant 按 user_id 过滤检索
  3. `add_note`：写入 `user_semantic_memory`
  4. 结果以 `AIMessage(name="MemoryAgent")` 追加到主图消息
- 回到 Supervisor → FINISH

**Step 3C — 路由到 GeneralAgent**
- `general_node` 被调用：
  1. 子图内 LLM 可调用 `get_stats` 或直接回答
  2. 结果以 `AIMessage(name="GeneralAgent")` 追加
- 回到 Supervisor → FINISH

**Step 4 — ask() 提取答案**
- 从 `result["messages"]` 末尾向前找第一条有内容的 AIMessage
- 返回其 `content` 作为最终回答

**多 Agent 串联示例**（Supervisor 不立即 FINISH 的情况）：
- 用户："帮我查一下论文里关于 Attention 的内容，然后把要点保存为笔记"
- 第一轮：Supervisor → ResearchAgent（检索内容）→ Supervisor
- 第二轮：Supervisor 看到检索结果，→ MemoryAgent（保存笔记）→ Supervisor
- 第三轮：Supervisor 确认两步都完成 → FINISH

---

#### 操作 4：在「学习笔记」Tab 保存笔记

与 app.py 相同，直接调用 `agent.add_note()`，绕过 Agent 图直接写入 Qdrant。

---

## 三、两种架构对比

| 维度 | app.py（ReAct + Plan-and-Execute） | multi_agent_app.py（Supervisor + Multi-Agent） |
|------|------|------|
| 路由方式 | LLM 通过工具描述自主选择工具 | Supervisor LLM 显式路由到专家 Agent |
| 工具隔离 | 所有工具在同一个 Agent 中 | 工具按职责分配给不同 Agent |
| 复杂任务 | Plan-and-Execute 在 Python 层串行分解 | Supervisor 可跨 Agent 多轮协调 |
| 幻觉检测 | 内置反思循环，最多重试 3 次 | 未内置（可在 ResearchAgent 子图中扩展） |
| LLM 调用次数 | 每次问答：1次规划 + N次ReAct + 1次幻觉检测 | 每次问答：1次Supervisor路由 + N次子Agent ReAct + 1次FINISH确认 |
| 适用场景 | 工具数量少、任务边界清晰 | 工具数量多、需要多专家协作、职责需要隔离 |
| 扩展方式 | 新增工具函数 | 新增专家 Agent 节点，不影响现有 Agent |
