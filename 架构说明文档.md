# Multi-Agent 文档问答助手 — 架构说明文档

## 一、项目结构

```
my_agent_project/
├── main.py                   # 核心协调类 MultiAgentApp，对外暴露 ask() / load_document()
├── config.py                 # LLM / FastLLM / Embedding 模型初始化
├── state.py                  # LangGraph 共享状态定义 SupervisorState
├── memory/
│   ├── store.py              # Qdrant 初始化、长期记忆读写
│   └── compression.py        # 短期记忆滑动窗口压缩
├── agents/
│   ├── base.py               # 通用 ReAct 子图工厂
│   ├── research.py           # ResearchAgent：PDF 检索问答
│   ├── memory_agent.py       # MemoryAgent：笔记管理
│   └── general.py            # GeneralAgent：闲聊与统计
├── graph/
│   ├── supervisor.py         # Supervisor 路由节点
│   └── builder.py            # 主图组装
├── document/
│   └── loader.py             # PDF 解析与向量入库
├── session/
│   └── manager.py            # Session 生命周期管理
└── ui/
    └── gradio_app.py         # Gradio 前端
```

---

## 二、整体架构图

```
用户浏览器
    │
    ▼
Gradio UI (ui/gradio_app.py)
    │  新建/恢复会话、发送消息、上传PDF
    ▼
MultiAgentApp (main.py)
    │
    ├─ ask(question)
    │      │
    │      ├─ 1. 滑动窗口检查 (memory/compression.py)
    │      ├─ 2. 长期记忆检索注入 (memory/store.py)
    │      └─ 3. invoke LangGraph 主图
    │                │
    │                ▼
    │         ┌─ Supervisor ─────────────────────────┐
    │         │  (graph/supervisor.py)               │
    │         │  判断路由 → ResearchAgent             │
    │         │           → MemoryAgent    ──────────┘
    │         │           → GeneralAgent   (回答后返回Supervisor)
    │         │           → FINISH → END
    │         └──────────────────────────────────────┘
    │
    └─ load_document(pdf_path)
           └─ document/loader.py → Qdrant pdf_knowledge
```

---

## 三、完整生命周期

### 3.1 启动阶段

1. `python main.py` 启动，`create_gradio_ui(app_factory=MultiAgentApp)` 构建 Gradio 界面
2. Gradio 在 `0.0.0.0:7861` 监听，用户打开浏览器看到左右布局界面
3. 此时 `MultiAgentApp` 尚未初始化，`state["app"] = None`

### 3.2 新建会话

用户点击"新建对话"按钮：

```
UI.new_session(user_id)
  → session/manager.py: create_session(user_id)
      → 生成 session_id = "session_YYYYMMDD_HHMMSS"
      → 写入 sessions.json（id、标题、创建时间）
  → MultiAgentApp.__init__(user_id, session_id=None)
      → config.py: 初始化 LLM (glm-5)、FastLLM (glm-4-flash)、Embedding (Embedding-3)
      → memory/store.py: init_qdrant() 连接 Qdrant，确保两个 collection 存在
      → sqlite3.connect("sessions.db") + SqliteSaver 初始化持久化 checkpointer
      → graph/builder.py: build_graph() 编译 LangGraph 主图
  → UI 刷新会话列表，右侧对话框清空
```

### 3.3 恢复历史会话

用户点击左侧历史会话条目：

```
UI.load_session(session_id)
  → MultiAgentApp.__init__(user_id, session_id=已有id)
      → 跳过 create_session，直接使用传入的 session_id
      → 同样初始化 LLM、Qdrant、SqliteSaver、主图
  → app.get_chat_history()
      → app.get_state(config) 从 sessions.db 读取该 thread_id 的消息历史
      → 过滤出 HumanMessage + 带 name 的 AIMessage，配对成 [[user, ai], ...]
  → UI 将历史消息渲染到对话框
```

### 3.4 用户发送消息（核心流程）

用户在输入框输入问题，点击发送：

```
UI.chat(message, history)
  → MultiAgentApp.ask(question)
```

**ask() 内部详细步骤：**

```
Step 1: 更新会话标题
  → 第一条消息时，取前20字写入 sessions.json 作为会话标题

Step 2: 滑动窗口压缩检查 (memory/compression.py)
  → app.get_state() 读取当前消息数
  → 若消息数 > WINDOW_SIZE(20)：
      → 取最早 COMPRESS_BATCH(4) 条消息
      → fast_llm 生成摘要 + 重要性评分(1-5)
      → 评分 >= PROMOTE_THRESHOLD(3)：save_fact() 写入 Qdrant (type=auto_fact)
      → RemoveMessage 删除旧消息，插入 SystemMessage 摘要
      → app.update_state() 写回 SqliteSaver

Step 3: 长期记忆检索注入 (memory/store.py)
  → memory_store.similarity_search(question, k=3)
      过滤条件：user_id + session_id 双重隔离
  → 若有结果，拼成 SystemMessage 注入 input_messages

Step 4: invoke 主图
  → input_messages = [SystemMessage(记忆上下文，可选), HumanMessage(问题)]
  → app.invoke({"messages": input_messages}, config={"thread_id": session_id})
  → SqliteSaver 自动将新消息追加写入 sessions.db
```

**主图执行流程（graph/builder.py + graph/supervisor.py）：**

```
START
  ↓
Supervisor 节点
  → 检查最后一条消息是否来自专家 Agent
      是 → 返回 FINISH → END
  → 否则取最后一条 HumanMessage
  → fast_llm 判断路由（只输出一个词）
      "你好/闲聊" → GeneralAgent
      "论文/文档" → ResearchAgent
      "笔记/记录" → MemoryAgent
  ↓
专家 Agent 节点（以 ResearchAgent 为例）
  → research_agent.invoke({"messages": state["messages"]})
      进入 ReAct 子图：
        agent_node:
          → 提取 state 中所有 SystemMessage 合并进系统提示（GLM兼容处理）
          → llm.bind_tools([search_pdf]).invoke([sys_msg] + conv_msgs)
          → LLM 决策：调用工具 or 直接回答
        should_continue:
          → 有 tool_calls 且未超过 max_tool_calls(1) → 执行工具
          → 否则 → END
        tools 节点:
          → 执行 search_pdf(query)
          → 返回工具结果，回到 agent_node
  → 取子图最终 AIMessage.content
  → 包装为 AIMessage(content=answer, name="ResearchAgent") 写入主图 state
  ↓
回到 Supervisor
  → 检测到最后一条消息来自 ResearchAgent → FINISH → END
```

**ask() 收尾：**

```
  → 从 result["messages"] 反向找最后一条有内容的 AIMessage
  → 返回 answer 给 UI
  → UI 将 [question, answer] 追加到 chatbot 显示
```

### 3.5 上传 PDF 文档

```
UI.load_pdf(pdf_file)
  → MultiAgentApp.load_document(pdf_path)
  → document/loader.py: load_document()
      → pymupdf4llm.to_markdown(pdf_path)  解析为 Markdown 文本
      → 父块切分：RecursiveCharacterTextSplitter(chunk_size=1500, overlap=200)
      → 子块切分：RecursiveCharacterTextSplitter(chunk_size=400, overlap=50)
      → 每个子块 metadata 记录 parent_id + parent_text + source + user_id
      → pdf_store.add_documents(child_docs)  写入 Qdrant pdf_knowledge collection
```

---

## 四、关键技术说明

### 4.1 多 Agent 编排：Supervisor + ReAct 架构

**框架**：LangGraph

**主图结构**（Supervisor 模式）：
- `SupervisorState` 是所有节点共享的状态，核心字段 `messages` 通过 `add_messages` reducer 自动累积
- Supervisor 节点负责意图识别和路由，不参与实际回答
- 三个专家 Agent 各自是独立的 ReAct 子图，完成后将最终回答以 `AIMessage(name=AgentName)` 写回主图
- Supervisor 检测到专家已回答（最后一条消息来自专家）时输出 FINISH，图执行结束

**ReAct 子图结构**（agents/base.py）：
```
START → agent_node ⇄ tools_node → END
```
- `agent_node`：LLM 决策，输出 tool_calls 或最终回答
- `tools_node`：LangChain `ToolNode` 执行工具调用
- `should_continue`：判断继续调用工具还是结束

**工具调用防循环机制**（双重保障）：
1. 系统提示层面：明确告知 LLM 最多调用 N 次工具
2. 代码层面：`should_continue` 统计 `ToolMessage` 数量，超过 `max_tool_calls` 强制返回 END

### 4.2 PDF 检索：MQE + 父子块 + LLM 重排

**父子块策略（Parent-Child Chunking）**：
- 父块（1500字）：保留完整上下文，用于最终回答生成
- 子块（400字）：粒度更细，用于向量检索，提高召回精度
- 子块 metadata 存储 `parent_id` 和 `parent_text`，检索命中子块后取回父块内容

**多查询扩展（MQE，Multi-Query Expansion）**：
- 用户原始问题 + fast_llm 生成 3 个不同表达的变体查询词
- 4 个查询词分别检索，合并去重，扩大召回覆盖面
- 解决单一查询词因表达差异导致的漏召回问题

**LLM 重排（Reranking）**：
- 召回的多个父块片段交给 fast_llm 进行相关性筛选
- 剔除无关片段，拼接最相关内容作为最终检索结果
- 比向量相似度排序更准确，能理解语义相关性

**向量数据库**：Qdrant，使用余弦相似度（COSINE），向量维度 2048（Embedding-3 模型）

### 4.3 记忆系统：短期 + 长期双层架构

**短期记忆（会话内）**：
- 存储介质：SQLite（`sessions.db`），通过 LangGraph `SqliteSaver` 管理
- 机制：`add_messages` reducer 自动累积，每轮 `invoke()` 后写透（write-through）
- 滑动窗口压缩：消息数超过 20 条时触发，压缩最早 4 条为摘要，用 `RemoveMessage` 删除旧消息后插入摘要，保持窗口可控

**长期记忆（持久化）**：
- 存储介质：Qdrant `user_semantic_memory` collection
- 来源一（自动）：滑动窗口压缩时，fast_llm 对被压缩内容打重要性评分（1-5），评分 ≥ 3 的内容自动晋升为 `auto_fact` 存入 Qdrant
- 来源二（手动）：用户通过 MemoryAgent 的 `add_note` 工具主动保存，类型为 `note`
- 检索注入：每轮 `ask()` 开始时，用当前问题语义检索 top-3 相关记忆，拼成 `SystemMessage` 注入对话上下文

**Session 隔离**：所有 Qdrant 记忆同时按 `user_id + session_id` 双重过滤，不同会话之间完全隔离

### 4.4 Session 管理

- **元数据持久化**：`sessions.json` 记录每个 session 的 id、用户、标题、创建时间
- **消息历史持久化**：`sessions.db`（SQLite）通过 `SqliteSaver` 存储 LangGraph checkpointer 状态
- **自动命名**：第一条用户消息的前 20 字自动作为会话标题
- **恢复机制**：传入已有 `session_id` 初始化 `MultiAgentApp`，LangGraph 自动从 SQLite 加载该 thread 的历史状态

### 4.5 模型分工

| 模型 | 用途 | 特点 |
|------|------|------|
| glm-5（LLM） | 专家 Agent 推理、工具调用决策、最终回答生成 | 能力强，用于核心推理 |
| glm-4-flash（FastLLM） | Supervisor 路由、MQE 查询扩展、LLM 重排、记忆压缩评分 | 速度快，用于辅助判断 |
| Embedding-3 | 文档向量化、记忆向量化、相似度检索 | 2048 维，余弦相似度 |

### 4.6 GLM 模型兼容性处理

GLM 系列模型有一个限制：`SystemMessage` 只能出现在消息列表最开头，不能夹在对话中间。

本项目中有两处会产生中间位置的 SystemMessage：
1. 长期记忆注入（每轮 ask() 追加到 state）
2. 滑动窗口压缩摘要（替换旧消息后插入）

**解决方案**（agents/base.py）：在 `agent_node` 调用 LLM 前，将 state 中所有 SystemMessage 提取出来合并进 agent 自己的系统提示，对话历史只传 Human/AI/Tool 消息，确保 SystemMessage 始终在最前面。

---

## 五、数据流向总览

```
PDF 文件
  → pymupdf4llm 解析
  → 父子块切分
  → Embedding-3 向量化
  → Qdrant pdf_knowledge collection

用户问题
  → 滑动窗口压缩（必要时）
  → 长期记忆检索（Qdrant user_semantic_memory）
  → LangGraph 主图
      → Supervisor 路由
      → 专家 Agent（ReAct：LLM + 工具）
          ResearchAgent: MQE → 向量检索 → 父块召回 → LLM 重排
          MemoryAgent:   Qdrant 检索 / 写入
          GeneralAgent:  直接 LLM 回答
      → 回答写回主图 state
  → SqliteSaver 持久化到 sessions.db
  → 返回最终回答给用户
```
