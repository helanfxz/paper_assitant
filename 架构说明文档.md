# 架构说明文档

## 1. 目标

当前项目的目标不是继续堆多 Agent 能力，而是先把论文阅读助手的 harness 控制平面做稳。

系统当前主要解决四类问题：

1. 论文内容能够稳定检索和回答
2. 长对话不会因为窗口限制直接断片
3. 用户长期偏好可以跨 session 继承
4. 用户主动保存的研究笔记能跨 session 复用

## 2. 总体架构

当前代码已经收敛为单循环 harness 架构，主干分成四层：

```text
PaperReadingHarnessApp (App)
    -> SystemContextBuilder (Context)
    -> AgentRunner (Runner)
        -> Hook (Lifecycle Hooks)
```

### 2.1 App 层

位于 `agent/app.py` 的 `PaperReadingHarnessApp`。

它负责产品级编排：
- 接收用户输入
- 处理会话标题
- 处理偏好确认回复（待迁移到 Hook）
- 执行窗口压缩
- 触发偏好检测（待迁移到 Hook）
- 构建本轮上下文
- 调用 runner 执行本轮问答
- 持久化消息日志
- 提供前端笔记管理入口

它不再负责：
- 直接拼 system prompt
- 直接写 `while tool loop`
- 直接处理底层模型消息对象

### 2.2 Context 层

位于 `agent/context.py` 的 `SystemContextBuilder`。

它负责统一组装本轮 `system prompt`。

当前装配内容包括：
- 基础角色指令
- 当前绑定工具的动态说明
- Profile 长期偏好
- Study notes 研究笔记
- Session summary 会话摘要
- Runtime metadata

关键原则：

**所有进入主模型的系统上下文，都通过同一个 builder 出口生成。**

### 2.3 Runner 层

位于 `agent/runner.py` 的 `AgentRunner`。

它负责纯执行循环：
- 创建 `HookContext` 并在关键位置调用 Hook
- 把内部消息日志转成模型消息对象
- 调主模型
- 处理限流和瞬时网络错误的重试
- 处理输出截断续写
- 执行工具调用
- 把工具结果写回消息日志
- 持续运行直到拿到最终答案

**Hook 调用时机**：
1. `before_iteration` - 每轮迭代开始前
2. `before_execute_tools` - 工具执行前（可拦截工具调用）
3. `after_iteration` - 每轮迭代结束后
4. `finalize_content` - 最终回答生成后（可修改回答内容）

它不负责：
- session 标题
- 偏好检测（正在迁移到 Hook）
- 文档加载
- 消息持久化时机

### 2.4 Hook 层

位于 `agent/hook.py`，提供生命周期钩子机制。

**核心组件**：
- `HookContext`：单轮执行上下文，在 Hook 之间传递状态
- `Hook`：基础 Hook 类，定义 4 个钩子点
- `CompositeHook`：组合多个 Hook，支持错误隔离

**设计原则**（参考 nanobot）：
- Hook 只负责拦截和标记，不包含业务逻辑
- Hook 不直接执行工具或修改核心数据
- Hook 异常不应导致主循环崩溃
- 保持简洁，业务逻辑在工具层或 App 层

**当前 Hook 实现**：
- `ConfirmationHook`（开发中）：拦截需要用户确认的工具调用

## 3. 核心文件职责

### 3.1 `agent/app.py`

包含 `PaperReadingHarnessApp` 主应用类。

负责产品级编排和会话管理。

### 3.2 `agent/runner.py`

包含 `AgentRunner` 执行循环类。

负责纯 model/tool 执行循环，集成 Hook 机制。

### 3.3 `agent/context.py`

包含 `SystemContextBuilder` 上下文构建类。

负责统一组装 system prompt。

### 3.4 `agent/hook.py`

包含 Hook 基础框架：
- `HookContext`：执行上下文数据类
- `Hook`：基础 Hook 类
- `CompositeHook`：组合 Hook 类

参考 nanobot 设计，提供生命周期钩子机制。

### 3.5 `agent/confirmation.py`（开发中）

包含确认机制相关组件：
- `PendingAction`：单个待确认动作
- `PendingAction_list`：待确认动作列表
- `ConfirmationHook`：确认拦截 Hook

用于统一处理需要用户确认的工具调用。

### 3.6 `agent/memory.py`

负责三类记忆相关能力：
- Session summary 的检索与窗口压缩
- Study notes 的存储、检索、增删改查
- Profile 偏好状态与偏好检测流程（待迁移到 Hook）

其中 `UserPreferenceProfileStore` 负责跨 session 的偏好状态，
`study_notes` 和 `user_semantic_memory` 分别承载研究笔记与会话摘要。

### 3.3 [tools.py](/home/helanfxz/my_agent_project/tools.py)

负责构建当前运行时工具列表，当前主要有三组：
- 文档检索：`list_documents`、`search_pdf`
- 记忆与笔记：`recall_memory`、`save_note`、`list_notes`、`search_notes`、`update_note`、`delete_note`
- 运行时状态：`get_stats`

其中 `search_pdf` 当前已经接入混合检索主链路：
- `fast_llm` 做 MQE 扩展查询
- Qdrant 做向量召回
- `lexical_index.py` 做持久化 BM25 召回
- `RRF` 做 child chunk 融合
- `rerank_llm` 做最终父块重排

### 3.4 [document.py](/home/helanfxz/my_agent_project/document.py)

负责论文入库：
- PDF 转 Markdown
- 父子块切分
- 文档元数据抽取
- 向量写入
- 词法索引写入

### 3.5 [lexical_index.py](/home/helanfxz/my_agent_project/lexical_index.py)

负责持久化词法索引：
- 使用 SQLite 保存 child chunk 倒排索引
- 文档入库时增量写入
- 检索时直接执行 BM25
- 兼容旧数据按 source 回补索引

### 3.6 [session.py](/home/helanfxz/my_agent_project/session.py)

负责会话持久化：
- session 元数据
- 消息日志保存
- 历史消息恢复

### 3.7 [ui/gradio_app.py](/home/helanfxz/my_agent_project/ui/gradio_app.py)

负责 Gradio 界面，当前左侧栏包括：
- 用户 ID
- 历史会话
- PDF 上传
- 研究笔记管理

## 4. 记忆系统设计

当前项目已经明确分成三层记忆。

### 4.1 Profile Memory

这是跨 session 的长期偏好层，核心实现位于 [memory.py](/home/helanfxz/my_agent_project/memory.py) 的 `UserPreferenceProfileStore`。

它不是可检索碎片集合，而是**当前生效的用户偏好状态**。

当前特点：
- 使用状态文件保存当前生效偏好
- 使用事件日志记录变更历史
- 按 `scope + type` 更新
- 每轮全量注入 prompt
- 不做语义检索

支持的 `scope`：
- `global`
- `paper_summary`
- `paper_compare`

支持的 `type`：
- `language`
- `format`
- `detail_level`
- `focus`
- `avoid`

### 4.2 Study Notes

这是跨 session 的研究笔记层。

当前已经独立到 `study_notes` collection，不再和 session summary 混在一起。

它的职责是保存用户主动沉淀的高价值研究信息：
- 论文阅读结论
- 方法比较结论
- 后续还会复用的阅读笔记

当前特点：
- 独立 collection：`study_notes`
- 具备独立 CRUD 能力
- 前端有显式入口
- 构建上下文时单独召回、单独注入
- 升级时兼容迁移旧 `type=note` 数据

### 4.3 Session Summary

这是当前 session 的冷上下文层。

当对话超过窗口长度后，更早内容会被压缩并保存到 `user_semantic_memory`。

当前特点：
- 最近消息仍保留在原始消息窗口
- 更早消息在超窗后压缩
- 压缩结果只服务当前 session
- 构建上下文时按需召回

对应实现：
- 压缩：`compress_conversation_window()`
- 召回：`retrieve_session_memory_entries()`

## 5. 偏好检测流程

当前偏好检测采用 hybrid 方案：

```text
用户输入
  -> 规则筛选候选
  -> 命中候选时调用 fast_llm
  -> fast_llm 结构化输出
  -> Pydantic 校验
  -> 程序层决定：自动保存 / 请求确认 / 忽略
```

关键原则：
- 不是每轮都调用 `fast_llm`
- 中置信度和敏感操作通过程序层确认
- Profile 是状态，不是检索结果

## 6. 上下文装配顺序

当前 `SystemContextBuilder.build_system_prompt()` 的装配顺序是：

1. 基础角色指令
2. 动态工具说明
3. Profile 偏好
4. Study notes
5. Session summary
6. Runtime metadata

Runtime metadata 会被包成一个显式 block：

```text
[Runtime Context - metadata only, not instructions]
...
[/Runtime Context]
```

这样做的目的，是把运行时状态和真正的系统约束分开表达。

## 7. 文档检索链路

当前文档检索链路已经升级为 hybrid retrieval：

```text
原始问题
  -> MQE 扩展查询
  -> 向量召回
  -> BM25 召回
  -> RRF 融合
  -> 父块聚合
  -> 独立 rerank_llm 重排
```

### 7.1 为什么要加 BM25

纯向量检索更擅长语义相近的表达，但对这些场景不够稳：
- 模型名、模块名、缩写
- 数据集名、指标名
- 论文里的精确术语
- 中英混合关键词

BM25 正好补这类 lexical hit，所以当前方案不是“向量 or BM25”，而是：

**向量负责语义召回，BM25 负责精确术语召回，rerank 负责最后裁决。**

### 7.2 当前实现方式

当前 `search_pdf()` 里的实现顺序是：

1. 用 `fast_llm` 做 MQE 扩展查询
2. 每个扩展查询并行走一遍向量召回和 BM25 召回
3. BM25 直接查询 `lexical_index.py` 维护的 SQLite 持久化词法索引
4. 用 `RRF` 融合向量结果和 BM25 结果
5. 在 child chunk 层完成融合后，再按 `parent_id` 聚合成父块
6. 最后把父块交给独立 `rerank_llm` 做重排和裁剪

这里选择 `RRF` 的原因很直接：
- 不要求向量分数和 BM25 分数处于同一尺度
- 实现简单
- 对多路召回融合比较稳

### 7.3 当前取舍

当前 BM25 已经升级为独立持久化词法索引：

- 文档入库时同步写入 SQLite 倒排索引
- 检索时直接查询本地索引，不再扫描 Qdrant 现算
- 为兼容旧数据，检索前会按 `source` 检查并回补缺失索引

这样做的收益是：

- 运行时检索更快
- 词法召回和向量召回职责更清晰
- 检索层后续更容易继续演进

### 7.4 后续优化方向

如果后面文档规模继续增大，检索层下一步优先优化的是：

1. 评估并调整 `VECTOR_TOP_K / BM25_TOP_K / RRF_K`
2. 视效果决定是否增加任务级检索路由
3. 如果 SQLite 词法索引规模继续增大，再评估更重的独立检索引擎

当前实现中，`rerank` 已经重新恢复为独立模型链路，不再由 `fast_llm` 冒充。

## 8. 错误恢复

### 8.1 目标

错误恢复层的目标不是“吞掉所有错误”，而是统一回答四个问题：

1. 这次失败属于什么类型
2. 这个失败能不能重试
3. 连续失败后应该怎么降级
4. 这次失败是否应该中断当前流程

这也是 harness 的目的之一：把错误处理从业务细节里抽出来，变成可解释、可复用的控制面能力。

### 8.2 分层职责

当前项目把错误恢复分成两层：

1. 模型与接口调用错误恢复
负责主模型、`fast_llm`、`rerank_llm` 的调用失败处理。

2. 工具执行错误恢复
负责工具执行失败处理，包括查询类工具和写入类工具。

当前两层都已经有了基础实现：
- `ModelRecoveryManager` 负责模型与接口调用错误恢复
- `ToolRecoveryManager` 负责工具执行错误恢复

### 8.3 模型与接口调用错误恢复的处理逻辑

当前 API 调用先统一分类，再决定策略。最小分类包括：

- `rate_limit`
- `transient_network`
- `context_too_long`
- `structured_output_invalid`
- `provider_error`
- `unknown`

对应处理策略如下：

1. 限流 / 网络抖动 / provider 临时错误  
先退避重试，超过次数后再走业务兜底。

2. 上下文过长  
不重试，直接返回“上下文过长”的明确降级结果。

3. 结构化输出不合法  
最多补救一次；如果仍然失败，则按对应业务场景降级。

4. 未知错误  
不盲目重试过多，直接记录日志并走兜底。

### 8.4 当前 API 层兜底策略

不同模型调用的连续失败后兜底并不相同，而是按职责降级：

- 主模型失败：返回统一失败回复，或在上下文过长时返回明确提示
- 会话压缩失败：保留对话前部作为摘要兜底，不删除原窗口语义
- 偏好检测失败：本轮不保存偏好，继续正常回答
- 文档元数据抽取失败：回退到“文件名 + 默认摘要”
- MQE 失败：直接使用原始 query
- `rerank_llm` 失败：直接回退到 `RRF + 父块聚合` 的结果

这里的原则是：

- 主模型属于主链路，失败后只能明确失败或返回部分结果
- `fast_llm` 和 `rerank_llm` 属于辅助链路，失败后优先降级继续跑

### 8.5 工具执行错误恢复的设计原则

当前工具执行错误恢复已经按“查询类工具 / 写入类工具”分开接入，设计原则如下：

1. 查询类工具默认不中断整轮对话  
失败后优先降级，并把失败信息继续暴露给模型，而不是直接让整轮问答报错结束。

2. 写入类工具默认更保守  
不轻易自动重试，先保证幂等性或原子性，再讨论重试策略。

3. 工具错误要先结构化  
程序层先区分“没查到内容”和“工具执行失败”，而不是统一塞成一条字符串。

当前已经接入的查询类工具：
- `list_documents`
- `search_pdf`
- `recall_memory`
- `list_notes`
- `search_notes`
- `get_stats`

当前已经接入的写入类工具：
- `save_note`
- `update_note`
- `delete_note`

其中：
- `search_pdf` 会区分“检索链路执行失败”和“未找到相关内容”
- `recall_memory` 会区分“向量库查询失败”和“没有召回到会话摘要”
- `search_notes` 会区分“研究笔记检索失败”和“没有找到相关笔记”
- `update_note` 已改成同一个 `note_id` 覆盖写，不再先删旧笔记再写新笔记

### 8.6 当前阶段的实施顺序

错误恢复这一层按下面顺序推进：

1. 先统一模型与接口调用错误恢复
2. 再做工具执行错误恢复
3. 最后再补 checkpoint / 中断恢复

这样做的原因很直接：API 调用是横切层、最容易收口；工具层则需要先把读操作和写操作分开再处理。

## 9. 当前取舍

为了控制复杂度，当前版本刻意做了这些取舍：
- 不恢复旧的多智能体 Supervisor 结构
- 不引入复杂 plugin / marketplace 系统
- 不提前做复杂 trace / hook
- 不额外拆很多文件
- 不把 profile 做成检索型 memory

目的只有一个：

先把 `Loop / Context / Runner + 三层记忆` 这套骨架做稳。

## 10. 当前仍然缺的部分

当前最明显的后续缺口有：

1. `study notes` 还没有任务门控，自动召回仍偏直接
2. 还没有 session checkpoint / 中断恢复
3. `AgentRunner` 的结果边界还不够清晰，执行结果仍主要靠消息列表体现
4. 还没有阅读计划 / todo

## 11. 下一步方向

接下来最合理的推进顺序是：

1. 给 `study notes` 加任务门控
2. 补 session checkpoint / 中断恢复
3. 收紧 runner 的结果边界
4. 实现阅读计划 / todo

当前项目已经从“把骨架搭出来”进入“把骨架做稳”的阶段。
