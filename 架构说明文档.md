# Multi-Agent 文档问答助手 — 架构说明文档

## 一、项目结构

```
my_agent_project/
├── main.py                   # 核心协调类 MultiAgentApp，对外暴露 ask() / load_document()
├── config.py                 # LLM / FastLLM / Embedding 模型初始化
├── state.py                  # LangGraph 共享状态定义 SupervisorState（含 plan/plan_step/step_results）
├── memory/
│   ├── store.py              # Qdrant 初始化、长期记忆读写
│   └── compression.py        # 短期记忆滑动窗口压缩
├── agents/
│   ├── base.py               # 通用 ReAct 子图工厂
│   ├── research.py           # ResearchAgent：PDF 检索问答（含 list_documents 工具）
│   ├── memory_agent.py       # MemoryAgent：笔记管理
│   └── general.py            # GeneralAgent：闲聊与统计
├── graph/
│   ├── supervisor.py         # Supervisor 路由节点（兼 Plan-and-Act 调度）
│   └── builder.py            # 主图组装（含 synthesizer 汇总节点）
├── document/
│   ├── loader.py             # PDF 解析与向量入库
│   └── registry.py           # 文档元数据持久化（documents.json）
├── session/
│   └── manager.py            # Session 生命周期管理
└── ui/
    └── gradio_app.py         # Gradio 前端
```

---

## 二、整体架构图

```
用户浏览器
    │
    ▼
Gradio UI (ui/gradio_app.py)
    │  新建/恢复会话、发送消息、上传PDF
    ▼
MultiAgentApp (main.py)
    │
    ├─ ask(question)
    │      │
    │      ├─ 1. 滑动窗口检查 (memory/compression.py)
    │      ├─ 2. 长期记忆检索注入 (memory/store.py)
    │      └─ 3. invoke LangGraph 主图
    │                │
    │                ▼
    │         ┌─ Supervisor ─────────────────────────────────────┐
    │         │  (graph/supervisor.py)                           │
    │         │  简单问题 → 直接路由到某个 Agent                   │
    │         │  复杂问题 → 生成 plan → 逐步调度 Agent             │
    │         │           → ResearchAgent                        │
    │         │           → MemoryAgent    ─────────────────────┘
    │         │           → GeneralAgent   (回答后返回Supervisor)
    │         │           → synthesizer（计划全部完成后汇总）→ END
    │         │           → FINISH → END（简单模式）
    │         └──────────────────────────────────────────────────┘
    │
    └─ load_document(pdf_path)
           └─ document/loader.py → Qdrant pdf_knowledge
                  └─ document/registry.py → documents.json（元数据）
```

---

## 三、完整生命周期

### 3.1 启动阶段

1. `python main.py` 启动，`create_gradio_ui(app_factory=MultiAgentApp)` 构建 Gradio 界面
2. Gradio 在 `0.0.0.0:7861` 监听，用户打开浏览器看到左右布局界面
3. 此时 `MultiAgentApp` 尚未初始化，`state["app"] = None`
4. 左侧会话列表自动加载 `sessions.json` 中 `pdf_user1` 的历史会话

### 3.2 新建会话

用户点击"新建对话"按钮：

```
UI.new_session(user_id)
  → session/manager.py: create_session(user_id)
      → 生成 session_id = "session_YYYYMMDD_HHMMSS"
      → 写入 sessions.json（id、user_id、标题="新对话"、创建时间）
  → MultiAgentApp.__init__(user_id, session_id=None)
      → config.py: 初始化 LLM（环境变量 LLM_MODEL_ID）、FastLLM（glm-4-flash）、Embedding（Embedding-3）
      → memory/store.py: init_qdrant()
          → 连接 Qdrant（QDRANT_URL 环境变量，默认 :memory:）
          → 确保 pdf_knowledge、user_semantic_memory 两个 collection 存在（向量维度 2048，COSINE）
      → 初始化 stats 字典（session_start、docs_loaded、questions_asked、notes_added）
      → 初始化 loaded_docs 列表（本次运行加载的文档名，供图内工具引用）
      → sqlite3.connect("sessions.db") + SqliteSaver 初始化持久化 checkpointer
      → graph/builder.py: build_graph()
          → make_research_agent(llm, fast_llm, pdf_store, loaded_docs)
          → make_memory_agent(llm, memory_store, user_id, session_id, stats)
          → make_general_agent(llm, stats)
          → build_supervisor_node(fast_llm)
          → 组装 StateGraph，编译并绑定 checkpointer
  → UI 刷新会话列表，右侧对话框清空，session_label 更新
```

### 3.3 恢复历史会话

用户点击左侧历史会话条目：

```
UI.load_session(session_id)
  → MultiAgentApp.__init__(user_id, session_id=已有id)
      → 跳过 create_session，直接使用传入的 session_id（打印"恢复会话"日志）
      → 同样初始化 LLM、Qdrant、SqliteSaver、主图（与新建流程完全一致）
  → app.get_chat_history()
      → config = {"configurable": {"thread_id": session_id}}
      → app.get_state(config) 从 sessions.db 读取该 thread_id 的 LangGraph checkpoint
      → 遍历 messages：HumanMessage 暂存为 pending_user
        遇到带 name 属性的 AIMessage（来自专家 Agent）且有内容 → 配对成 [user, ai]
      → 返回 [[user1, ai1], [user2, ai2], ...] 格式
  → UI 将历史消息渲染到 chatbot 组件
```

### 3.4 用户发送消息（核心流程）

用户在输入框输入问题，按 Enter 或点击发送：

```
UI.chat(message, history)
  → 检查 state["app"] 是否为 None（未初始化则返回错误提示）
  → MultiAgentApp.ask(question)
```

**ask() 内部详细步骤：**

```
Step 0: 统计计数
  → stats["questions_asked"] += 1
  → config = {"configurable": {"thread_id": session_id}}

Step 1: 更新会话标题
  → 若 _first_message == True：
      → session/manager.py: update_session_title(session_id, question)
          → 读取 sessions.json，若标题仍为"新对话"则取 question[:20] 写入
      → _first_message = False

Step 2: 滑动窗口压缩检查 (memory/compression.py: compress_window)
  → app.get_state(config) 读取当前 checkpoint 中的完整 messages 列表
  → 若 len(messages) <= WINDOW_SIZE(20)：直接跳过，不压缩
  → 若超出：

    【取压缩目标】
      → conv_msgs = [m for m in messages if isinstance(m, (HumanMessage, AIMessage))]
      → old_msgs = conv_msgs[:COMPRESS_BATCH(4)]
        即最早的4条 HumanMessage/AIMessage（约2轮对话）
      → 若 len(conv_msgs) < COMPRESS_BATCH(4)：对话消息不足，跳过

    【拼接对话文本】
      → 遍历 old_msgs，跳过 content 为空的消息
      → HumanMessage → "用户: {content[:300]}"
      → AIMessage    → "AI: {content[:300]}"

    【fast_llm 生成摘要】
      → prompt 要求只输出一句话摘要
      → 异常兜底：summary = dialogue[:100]

    【写入长期记忆（全量）】
      → memory/store.py: save_fact(memory_store, summary, user_id, session_id, "auto_fact")
          → 构造 Document：
              page_content = summary（一句话摘要）
              metadata = {user_id, session_id, type: "auto_fact", timestamp}
          → memory_store.add_documents([doc]) 写入 Qdrant user_semantic_memory
      → 不再使用评分机制，全量写入；向量检索的相关性排序天然过滤低价值内容

    【删除消息】
      → removes = [RemoveMessage(id=m.id) for m in old_msgs]
          RemoveMessage 是 LangGraph 特殊消息类型，
          add_messages reducer 遇到它会从列表中删除对应 id 的消息
      → app.update_state(config, {"messages": removes})
          结果：messages 减少4条，不插入摘要 SystemMessage

    【压缩前后对比示例】
      压缩前（22条）：
        [0] HumanMessage("什么是LoRA")              ← conv_msgs[0]
        [1] AIMessage(name="ResearchAgent", "...")  ← conv_msgs[1]
        [2] HumanMessage("它的优势是什么")           ← conv_msgs[2]
        [3] AIMessage(name="ResearchAgent", "...")  ← conv_msgs[3]
        [4..21] 后续对话消息...

      old_msgs = [0][1][2][3]（conv_msgs 前4条）

      压缩后（18条）：
        [0..17] 后续对话消息（原[4..21]）
      → 打印压缩日志：22 → 18 条消息

Step 3: 长期记忆检索注入 (memory/store.py: build_memory_context)
  → memory_store.similarity_search(question, k=3,
        filter={"must": [user_id过滤, session_id过滤]})
  → 若有结果：
      → 拼接为 "【本会话的长期记忆】：\n- [type] content\n..."
      → input_messages.append(SystemMessage(content=context))
      → 打印注入日志（前80字）
  → input_messages.append(HumanMessage(content=question))

Step 4: invoke 主图
  → app.invoke(
        {"messages": input_messages, "plan": [], "plan_step": 0, "step_results": []},
        config=config
    )
  → SqliteSaver 自动将新消息追加写入 sessions.db（write-through）
```

**主图执行流程（graph/builder.py + graph/supervisor.py）：**

```
START
  ↓
supervisor 节点（build_supervisor_node）
  读取 state: plan, plan_step, step_results, 最后一条消息

  ── 无计划模式（plan 为空）──
  → 若最后一条消息来自专家 Agent（name in AGENT_NAMES）：
      → 返回 {"next": "FINISH"} → END
  → 否则取最后一条 HumanMessage 内容
  → 调用 document/registry.py: format_doc_list() 获取知识库文档列表
  → fast_llm.invoke(prompt) 判断简单/复杂：
      prompt 包含：知识库文档信息 + 用户问题 + 判断规则
      简单问题 → 输出一个词：GeneralAgent / ResearchAgent / MemoryAgent
      复杂问题（多文档/对比/多步骤）→ 输出 JSON：{"plan": ["Agent: 任务描述", ...]}
  → _try_parse_plan(raw)：尝试解析 JSON
      成功且 plan 长度 > 1 → 计划模式：
          → 返回 {"next": first_agent, "plan": parsed_plan, "plan_step": 0, "step_results": []}
      失败 → 简单路由：
          → 在 raw 中匹配 Agent 名称（优先级：ResearchAgent > MemoryAgent > GeneralAgent）
          → 返回 {"next": next_agent, "plan": [], "plan_step": 0, "step_results": []}

  ── 计划执行模式（plan 不为空）──
  → 若最后一条消息来自专家 Agent（刚完成一步）：
      → new_results = step_results + [last_agent.content]
      → new_step = plan_step + 1
      → 若 new_step < len(plan)：
          → _parse_plan_step(plan[new_step]) 提取下一步 Agent 名称
          → 返回 {"next": next_agent, "plan_step": new_step, "step_results": new_results}
      → 若全部完成：
          → 返回 {"next": "synthesizer", "plan_step": new_step, "step_results": new_results}
  → 若计划已生成但尚未开始（无 Agent 消息）：
      → 路由到 plan[0] 对应的 Agent

  ↓（条件边根据 state["next"] 分发）

专家 Agent 节点（以 ResearchAgent 为例，graph/builder.py: _invoke_agent）
  → 复制 state["messages"]
  → 若处于计划模式（plan 不为空）：
      → 提取 plan[plan_step] 中冒号后的任务描述
      → 在 messages 最前面插入 SystemMessage(content="【当前任务】：{task_desc}")
  → research_agent.invoke({"messages": messages})
      进入 ReAct 子图（agents/base.py: build_sub_agent）：
        agent_node:
          → 提取 state["messages"] 中所有 SystemMessage，合并进 agent 系统提示（GLM兼容）
          → conv_msgs = 过滤掉 SystemMessage 的对话历史
          → merged_sys = SystemMessage(constrained_prompt + extra_system_content)
          → llm_with_tools.invoke([merged_sys] + conv_msgs)
          → 若有 tool_calls → 打印工具名和参数
          → 若无 tool_calls → 打印"生成最终回答"
          → 返回 {"messages": [response]}
        should_continue:
          → 统计 state["messages"] 中 ToolMessage 数量（已执行次数）
          → 若最后一条有 tool_calls 且 tool_call_count < max_tool_calls(3) → "tools"
          → 若已达上限 → 打印强制结束日志 → END
          → 若无 tool_calls → END
        tools 节点（ToolNode）:
          → 执行对应工具（search_pdf / list_documents）
          → 返回 ToolMessage，回到 agent_node
  → 取子图 result["messages"][-1].content 作为回答
  → 包装为 AIMessage(content=answer, name="ResearchAgent") 写入主图 state
  ↓
回到 supervisor 节点
  → 检测到最后一条消息来自 ResearchAgent（简单模式）→ FINISH → END
  → 或推进计划步骤（计划模式）

synthesizer 节点（仅计划模式，graph/builder.py: synthesizer_node）
  → 读取 step_results（各步骤 Agent 的回答列表）
  → 读取 plan（各步骤描述）
  → 从 messages 中找最后一条 HumanMessage（用户原始问题）
  → 拼接 steps_summary：每步骤编号 + 计划描述 + 检索结果
  → llm.invoke(prompt) 生成综合回答（使用主 LLM，非 fast_llm）
  → 返回：
      {"messages": [AIMessage(content=answer, name="Synthesizer")],
       "plan": [], "plan_step": 0, "step_results": []}  ← 清空计划状态
  → synthesizer → END
```

**ask() 收尾：**

```
  → 遍历 result["messages"] 反向查找最后一条有内容的 AIMessage
  → 打印最终回答（前200字）
  → 返回 answer 给 UI
  → UI: history.append([message, response])，chatbot 组件更新显示
```

### 3.5 上传 PDF 文档

```
UI.load_pdf(pdf_file)
  → 检查 state["app"] 是否为 None
  → MultiAgentApp.load_document(pdf_path)
  → document/loader.py: load_document(pdf_path, pdf_store, user_id, fast_llm)
      → 检查文件是否存在
      → pymupdf4llm.to_markdown(pdf_path)  解析为 Markdown 文本
      → parent_splitter（chunk_size=1500, overlap=200）切分父块
      → child_splitter（chunk_size=400, overlap=50）对每个父块再切子块
      → 每个子块 metadata：
          parent_id（uuid4）、parent_text（父块全文）、source（文件名）、user_id
      → pdf_store.add_documents(child_docs)  写入 Qdrant pdf_knowledge collection
      → 若 fast_llm 不为 None 且文档未注册：
          → document/loader.py: _register_metadata(filename, md_text, chunk_count, fast_llm)
              → 取 md_text[:3000] 作为摘录
              → fast_llm.invoke(prompt) 提取标题和摘要（严格格式输出）
              → document/registry.py: register_document(filename, title, summary, chunk_count)
                  → 写入 documents.json（filename/title/summary/date_added/chunk_count）
      → 返回 {"success": True, "message": "解析成功 (耗时: Xs)，入库 N 个子块", "document": filename}
  → stats["docs_loaded"] += 1
  → loaded_docs.append(doc_name)（若未重复）
  → UI 显示 "✅ 解析成功..." 或 "❌ 错误信息"
```

---

## 四、关键技术说明

### 4.1 多 Agent 编排：Supervisor + Plan-and-Act + ReAct 架构

**框架**：LangGraph

**主图结构**（Supervisor 模式）：
- `SupervisorState` 是所有节点共享的状态，核心字段：
  - `messages`：通过 `add_messages` reducer 自动累积
  - `next`：Supervisor 写入，控制路由
  - `plan`：复杂任务时生成的步骤列表（格式："AgentName: 任务描述"）
  - `plan_step`：当前执行到第几步
  - `step_results`：各步骤执行结果，最终由 synthesizer 汇总
- Supervisor 节点负责意图识别和路由，不参与实际回答
- 三个专家 Agent 各自是独立的 ReAct 子图，完成后将最终回答以 `AIMessage(name=AgentName)` 写回主图
- 简单模式：Supervisor 检测到专家已回答时输出 FINISH，图执行结束
- 计划模式：Supervisor 逐步调度多个 Agent，全部完成后进入 synthesizer 汇总

**图结构**：
```
简单模式：START → supervisor → Agent → supervisor → FINISH → END
计划模式：START → supervisor → Agent → supervisor → Agent → ... → synthesizer → END
```

**ReAct 子图结构**（agents/base.py）：
```
START → agent_node ⇄ tools_node → END
```
- `agent_node`：LLM 决策，输出 tool_calls 或最终回答
- `tools_node`：LangChain `ToolNode` 执行工具调用
- `should_continue`：判断继续调用工具还是结束

**工具调用防循环机制**（双重保障）：
1. 系统提示层面：明确告知 LLM 最多调用 N 次工具
2. 代码层面：`should_continue` 统计 `ToolMessage` 数量，超过 `max_tool_calls` 强制返回 END

**各 Agent 工具配置**：
| Agent | 工具 | max_tool_calls |
|-------|------|----------------|
| ResearchAgent | list_documents, search_pdf | 3 |
| MemoryAgent | recall_memory, add_note | 1 |
| GeneralAgent | get_stats | 1 |

### 4.2 PDF 检索：MQE + 父子块 + LLM 重排

**父子块策略（Parent-Child Chunking）**：
- 父块（1500字）：保留完整上下文，用于最终回答生成
- 子块（400字）：粒度更细，用于向量检索，提高召回精度
- 子块 metadata 存储 `parent_id` 和 `parent_text`，检索命中子块后取回父块内容

**多查询扩展（MQE，Multi-Query Expansion）**：
- 用户原始问题 + fast_llm 生成 3 个不同表达的变体查询词
- 4 个查询词分别检索，合并去重，扩大召回覆盖面
- 解决单一查询词因表达差异导致的漏召回问题

**LLM 重排（Reranking）**：
- 召回的多个父块片段交给 fast_llm 进行相关性筛选
- 剔除无关片段，拼接最相关内容作为最终检索结果
- 比向量相似度排序更准确，能理解语义相关性

**文档注册（document/registry.py）**：
- 每次 PDF 入库后，fast_llm 从文档前 3000 字提取标题和摘要
- 注册到 `documents.json`（filename/title/summary/date_added/chunk_count）
- Supervisor 路由时通过 `format_doc_list()` 获取知识库文档列表，辅助判断是否需要检索
- ResearchAgent 的 `list_documents` 工具也从此处读取；若 documents.json 为空则直接扫描 Qdrant

**向量数据库**：Qdrant，使用余弦相似度（COSINE），向量维度 2048（Embedding-3 模型）

### 4.3 记忆系统：短期 + 长期双层架构

**短期记忆（会话内）**：
- 存储介质：SQLite（`sessions.db`），通过 LangGraph `SqliteSaver` 管理
- 机制：`add_messages` reducer 自动累积，每轮 `invoke()` 后写透（write-through）
- **滑动窗口压缩**：消息总数超过 20 条时触发，具体流程：
  1. 从 messages 中取出 `HumanMessage` 和 `AIMessage`，取最早的4条（`COMPRESS_BATCH=4`）作为压缩目标
  2. `fast_llm` 生成一句话摘要，全量写入 Qdrant 长期记忆（`type="auto_fact"`）
  3. 用 `RemoveMessage` 删除这4条消息，不插入摘要 SystemMessage
  4. 净效果：每次压缩消息数减少4条，历史信息通过每轮检索注入保证不丢失
  - 不再使用评分机制：LLM 评分不稳定，且向量检索本身具备相关性过滤能力，闲聊内容因语义距离远自然不会被召回

**长期记忆（持久化）**：
- 存储介质：Qdrant `user_semantic_memory` collection
- 来源一（自动）：滑动窗口压缩时，fast_llm 生成摘要全量写入，类型为 `auto_fact`
- 来源二（手动）：用户通过 MemoryAgent 的 `add_note` 工具主动保存，类型为 `note`
- 检索注入：每轮 `ask()` 开始时，用当前问题语义检索 top-10 候选记忆，综合相似度（权重0.7）和近期性（权重0.3）计算得分排序，过滤低于阈值0.5的，取 top-3 注入上下文；prompt 中按类型分组展示（事实 / 笔记），便于 LLM 区分优先级

**Session 隔离**：所有 Qdrant 记忆同时按 `user_id + session_id` 双重过滤，不同会话之间完全隔离

### 4.4 Session 管理

- **元数据持久化**：`sessions.json` 记录每个 session 的 id、user_id、标题、创建时间
- **消息历史持久化**：`sessions.db`（SQLite）通过 `SqliteSaver` 存储 LangGraph checkpointer 状态
- **自动命名**：第一条用户消息的前 20 字自动作为会话标题（仅当标题仍为"新对话"时更新）
- **恢复机制**：传入已有 `session_id` 初始化 `MultiAgentApp`，LangGraph 自动从 SQLite 加载该 thread 的历史状态
- **历史渲染**：`get_chat_history()` 只配对 HumanMessage 和带 `name` 属性的 AIMessage（专家 Agent 回答），过滤掉 SystemMessage、ToolMessage 等中间消息

### 4.5 模型分工

| 模型 | 用途 | 特点 |
|------|------|------|
| LLM（环境变量 LLM_MODEL_ID） | 专家 Agent 推理、工具调用决策、最终回答生成、synthesizer 汇总 | 能力强，用于核心推理 |
| glm-4-flash（FastLLM） | Supervisor 路由/计划生成、MQE 查询扩展、LLM 重排、记忆压缩摘要、文档元数据提取 | 速度快，用于辅助判断 |
| Embedding-3 | 文档向量化、记忆向量化、相似度检索 | 2048 维，余弦相似度 |

### 4.6 GLM 模型兼容性处理

GLM 系列模型有一个限制：`SystemMessage` 只能出现在消息列表最开头，不能夹在对话中间。

本项目中长期记忆注入会产生中间位置的 SystemMessage（每轮 ask() 追加到 state）。

**解决方案**（agents/base.py）：在 `agent_node` 调用 LLM 前，将 state 中所有 SystemMessage 提取出来合并进 agent 自己的系统提示，对话历史只传 Human/AI/Tool 消息，确保 SystemMessage 始终在最前面。

---

## 五、数据流向总览

```
PDF 文件
  → pymupdf4llm 解析
  → 父子块切分
  → Embedding-3 向量化
  → Qdrant pdf_knowledge collection
  → fast_llm 提取标题/摘要 → documents.json

用户问题
  → 滑动窗口压缩（必要时）→ Qdrant user_semantic_memory（auto_fact）
  → 长期记忆检索（Qdrant user_semantic_memory）→ SystemMessage 注入
  → LangGraph 主图
      → Supervisor 路由（简单/计划模式）
      → 专家 Agent（ReAct：LLM + 工具）
          ResearchAgent: list_documents → MQE → 向量检索 → 父块召回 → LLM 重排
          MemoryAgent:   recall_memory（Qdrant检索）/ add_note（Qdrant写入）
          GeneralAgent:  get_stats / 直接 LLM 回答
      → 回答写回主图 state（AIMessage with name）
      → 计划模式：synthesizer 汇总各步骤结果
  → SqliteSaver 持久化到 sessions.db
  → 返回最终回答给用户
```
